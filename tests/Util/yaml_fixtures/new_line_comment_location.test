security:
    providers:
        in_memory: { memory: ~ }
===
$data['security'] = ['encoders' => true] + $data['security'];
===
security:
    encoders: true
    providers:
        in_memory: { memory: ~ }
caddy:
    ports:
      - "foo:foo"
    volumes:
      - caddy_data
===
$data['caddy']['ports'][] = 'bar:baz';
===
caddy:
    ports:
      - "foo:foo"
      - 'bar:baz'
    volumes:
      - caddy_data
/*
* Here is an example about how configurate requests from an API with Axios.
* You can use make:react:api for generating your own resource dynamically.
* Install Axios: 
*   Using npm: npm install axios
*   Using yarn: yarn add axios
*/

/*
import axios from "axios";

const BASE_URL = 'http://127.0.0.1:8000';

const findAll = () => {
    return axios.get(`${BASE_URL}/api/resources`)
        .then(response => response.data);
}

const create = (resource) => {
    axios.post(`${BASE_URL}/api/resources`, resource);
}

const find = (id) => {
    return axios.get(`${BASE_URL}/api/resources/${id}`)
        .then(response => response.data);
}

const update = (id, resource) => {
    axios.put(`${ BASE_URL}/api/resources/${id}`, resource);
}

const remove = (id) => {
    axios.delete(`${BASE_URL}/api/resources/${id}`);
}

export default { findAll, find, create, update, remove };

*/